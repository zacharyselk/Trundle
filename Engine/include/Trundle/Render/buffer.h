#pragma once

#include <Trundle/common.h>
#include <Trundle/Render/renderer.h>


namespace Trundle {

  class IndexBuffer {
  public:
    static IndexBuffer
      IndexBuffer(const Renderer &r)
      : self(make_shared<IndexBuffer(T))

      create(const Renderer& r, uint32_t* indices, uint32_t size) const;;

}
